[package]
name = "extreme_bevy"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = "0.8"
ggrs = "0.9"
bevy_ggrs = { version = "0.10", features = ["wasm-bindgen"] }
matchbox_socket = { version = "0.4", features = ["ggrs-socket"] }