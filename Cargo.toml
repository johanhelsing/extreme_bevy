[package]
name = "extreme_bevy"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = "0.9"
# TODO: update after bevy_ggrs release
# bevy_ggrs = { version = "0.10", features = ["wasm-bindgen"] }
bevy_ggrs = { git = "https://github.com/Vrixyz/bevy_ggrs", branch = "pr/johanhelsing/36", features = ["wasm-bindgen"] }
matchbox_socket = { version = "0.4", features = ["ggrs-socket"] }

# TODO: remove patches after new bevy_ggrs release
[patch.crates-io]
ggrs = { git = "https://github.com/gschup/ggrs" }